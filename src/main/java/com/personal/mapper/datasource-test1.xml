<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.personal.datasource.datasource_test1">
    <select id="select" parameterType="java.lang.Long" resultType="java.util.HashMap">
        select * from test1_table where id = #{id}
    </select>
    <select id="select_dual" resultType="java.lang.Long">
        select 1 from dual
    </select>
</mapper>